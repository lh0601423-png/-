---
name: themasterplan
description: Apply TheMasterplan to delivery work in a repository that has adopted it.
---

# TheMasterplan for OpenCode

Read the repository root `AGENTS.md` and follow its Context Router.

OpenCode itself is only an execution harness; it does not make the task
`ABSTAINED`. If another delivery workflow owns the task lifecycle, follow the
repository abstention rule.

Load only task-relevant guidance, then continue within the authorized scope
until the repository Completion Contract is satisfied or a genuine human
decision boundary is reached.
